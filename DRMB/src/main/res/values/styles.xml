<resources>

<color name="seventies_gray">#FF343436</color>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat">
        <!-- Customize your theme here. -->
        <item name="android:background">@color/seventies_gray</item>
        <item name="android:windowBackground">@color/seventies_gray</item>
        <item name="android:colorBackground">@color/seventies_gray</item>
    </style>

    <style name="NavDrawerItemSelected" parent="AppTheme">
        <item name="android:activatedBackgroundIndicator">@drawable/activated_background</item>
    </style>

</resources>
